package com.unimedTeste.Unimed.Plano.repository;

import com.unimedTeste.Unimed.Plano.entity.Plano;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlanoRespository extends JpaRepository<Plano, Long> {
}
